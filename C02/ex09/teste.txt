#include <unistd.h>

char *ft_strcapitalize(char *str)
{
    while (*str != '\0')
    {
        if (*str >= 32 && *str <= 126)
        {                          
        if (*str == ' ')       
            write (1, "\n", 1);
            //*str = *str - 32; bem que ele podia entender que é para pular uma linha depois do ' ' ou 32 para depoius eu tentar fazer ele so a primeira letra maiuscula mas né 
        }
        str++;
    }
    return (str);
}

int main(void)
{
    char str[] = "hi, how are you? 42words forty-two; fifty+and+one";
    ft_strcapitalize(str);

    int i = 0;

    while (str[i])
    {
    write (1, &str[i], 1);
    i++;
    }
    write (1, "\n", 1);
    return (0);
}
